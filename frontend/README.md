# Frontend

Interfaz simple en HTML, CSS y JavaScript puro.

## Uso
Abre `frontend/index.html` en el navegador y asegurate de tener el backend corriendo en `http://localhost:8000`.

Si necesitas cambiar la URL, edita `API_BASE` en `frontend/app.js`.
